"""Authority / access-control primitives.

This package provides a clean separation between:
- Decision layer (Strategy / Intel), which determines LONG/SHORT/HOLD
- Execution layer (tool bot), which validates authorizations and executes
"""
